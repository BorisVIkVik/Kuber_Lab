apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: resourcemanager-claim0
  name: resourcemanager-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
