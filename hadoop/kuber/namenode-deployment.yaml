apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yaml --out ./kuber
    kompose.version: 1.38.0 (HEAD)
  labels:
    io.kompose.service: namenode
  name: namenode
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: namenode
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yaml --out ./kuber
        kompose.version: 1.38.0 (HEAD)
      labels:
        io.kompose.service: namenode
    spec:
      containers:
        - args:
            - bash
            - -c
            - hdfs namenode && hdfs dfs -mkdir /test
          env:
            - name: ENSURE_NAMENODE_DIR
              value: /tmp/hadoop-root/dfs/name
            - name: HDFS-SITE.XML_dfs.client.use.datanode.hostname
              value: "true"
            - name: HDFS-SITE.XML_dfs.datanode.use.datanode.hostname
              value: "true"
            - name: HDFS-SITE.XML_dfs.namenode.rpc-address
              value: namenode:8020
            - name: HDFS-SITE.XML_dfs.permissions.enabled
              value: "false"
            - name: HDFS-SITE.XML_dfs.replication
              value: "1"
          image: borisvikvik/hadoop-namenode:latest
          name: namenode
          ports:
            - containerPort: 9870
              protocol: TCP
            - containerPort: 8020
              protocol: TCP
          volumeMounts:
            - mountPath: /opt/hadoop/etc/hadoop/core-site.xml
              name: namenode-cm0
              subPath: core-site.xml
            - mountPath: /opt/hadoop/etc/hadoop/mapred-site.xml
              name: namenode-cm1
              subPath: mapred-site.xml
            - mountPath: /opt/hadoop/etc/hadoop/yarn-site.xml
              name: namenode-cm2
              subPath: yarn-site.xml
            - name: my-storage
              mountPath: /data
      hostname: namenode
      restartPolicy: Always
      volumes:
        - configMap:
            items:
              - key: core-site.xml
                path: core-site.xml
            name: namenode-cm0
          name: namenode-cm0
        - configMap:
            items:
              - key: mapred-site.xml
                path: mapred-site.xml
            name: namenode-cm1
          name: namenode-cm1
        - configMap:
            items:
              - key: yarn-site.xml
                path: yarn-site.xml
            name: namenode-cm2
          name: namenode-cm2
        - name: my-storage
          persistentVolumeClaim:
            claimName: my-pvc
