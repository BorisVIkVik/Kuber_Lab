apiVersion: batch/v1
kind: Job
metadata:
  name: example-job
spec:
  template:
    spec:
      containers:
      - name: busybox
        image: busybox:latest
        command: ["echo", "Job completed successfully!"]
      restartPolicy: Never
